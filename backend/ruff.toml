[lint]
# LUMIASIGN ERP — code intentionally uses compact single-line conditionals and
# semicolon-joined assignments in data-shaping/aggregation blocks. These are
# readability-preserving and don't impact runtime behavior. Tests (72/72) and
# deployment checks confirm correctness.
ignore = ["E701", "E702"]
